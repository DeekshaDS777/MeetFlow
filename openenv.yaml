name: meetflow_phase3
version: 3.0.0
description: Multi-stage meeting workflow environment with hybrid extraction and LLM decision making.
entrypoint:
  module: server.app
  app: app
metadata:
  domain: meeting_operations
  long_horizon: true
  reward_range: [0.01, 0.99]
  tasks: [easy, medium, hard]
api:
  reset: /reset
  step: /step
  state: /state
models:
  action: models.Action
  observation: models.Observation
  reward: models.Reward
